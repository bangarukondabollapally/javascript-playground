# Operators - Practice Challenges

## Challenge 1
Predict the output:
```js
console.log("10" + 1);
console.log("10" - 1);
console.log(true + false);
console.log(!!"Sheryians");
```

## Challenge 2
Convert to number using unary + operator
```js
let str = "42";
console.log(str);
```

## Challenge 3
A person is an "Adult" if their age is 18 or above, otherwise "Minor".

Print the correct message for `age = 17` using the **ternary operator**.

## Challenge 4
Build a calculator using the **ternary operator** (no `switch`, no `if/else`).

```js
function calc(a, b, operator) {
// +, -, *, /
}
```

## Challenge 5
Score logic — using **chained ternary operators** (no `if/else`):
```js
let marks = 82;
// Print "Excellent", "Good", "Average", or "Fail" based on ranges
```